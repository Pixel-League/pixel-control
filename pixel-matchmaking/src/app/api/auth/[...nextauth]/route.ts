import { handlers } from "@/auth";

/**
 * Auth.js v5 API route handler.
 * Handles /api/auth/signin, /api/auth/callback/maniaplanet, /api/auth/signout, etc.
 */
export const { GET, POST } = handlers;
