# Local-only sandboxes for reference experimentation.
# Use this instead of running mutable workflows inside `ressources/`.
.local-dev/
pixel-control-plugin/logs
pixel-control-server/logs
.history/

# Docker data (bind-mount volumes)
pixel-control-server/data/
pixel-sm-server/data/

# Dev UI (test tooling only, not production code)
pixel-control-ui/