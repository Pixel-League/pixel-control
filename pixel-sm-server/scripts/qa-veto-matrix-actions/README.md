# Deprecated - QA Veto Matrix Action Specs

This directory is kept for transition compatibility only.

Use `scripts/veto-action-matrix-steps/` for all new matrix-step changes.

It was previously consumed by `scripts/qa-veto-payload-sim.sh matrix`.
